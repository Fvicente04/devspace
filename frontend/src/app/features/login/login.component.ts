// Login page placeholder — replaced in Phase 2 with GitHub OAuth redirect
import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  template: '<p>Login page</p>',
})
export class LoginComponent {}
